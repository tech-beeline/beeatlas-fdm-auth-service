-- ════════════════════════════════════════════════════════════════════
--  SFDM-3976 (доп. по замечаниям тестирования): BI создаётся,
--  редактируется и удаляется без проверки членства в продукте — как
--  и CJ. Раньше было реализовано только "продукт необязателен"
--  (SFDM-3976, V0028), но ограничение по чужому product всё ещё
--  срабатывало (403), если продукт указан.
--
--  Группы 3 (BiBodyProductGroup, POST), 4 (BiPatchGroup, PATCH),
--  5 (BiDeleteGroup, DELETE) используются исключительно для BI create/
--  patch/delete (проверено во всех генерациях путей: V0010, V0012,
--  V0016 — только business-interactions/bi роуты). Группа 2
--  (BiProductGroup, GET по id) не трогается — просмотр черновика по
--  членству в продукте остаётся как было, это не входит в текущий
--  репорт тестировщика.
-- ════════════════════════════════════════════════════════════════════

UPDATE user_auth.policy
SET check_group_id = NULL
WHERE check_group_id IN (3, 4, 5);
