-- ════════════════════════════════════════════════════════════════════
--  SFDM-4256 (доп. по замечаниям тестирования): GET BI по id должен
--  отдавать черновик сотрудникам из любого продукта, а не только из
--  продукта, которому принадлежит BI.
--
--  Ранее (V0031) группа 2 (BiProductGroup, GET по id) была намеренно
--  не тронута — просмотр черновика по членству в продукте требовался,
--  это не входило в тот репорт. Сейчас тестировщик просит открыть
--  именно этот случай: сотрудник (роль DEFAULT) из чужого продукта
--  должен видеть черновик. BI_PRODUCT_MEMBER (group 2) используется
--  только для GET /{id} (v1 и v2, во всех вариантах путей gateway:
--  V0010, V0012, V0016) — снимаем ограничение полностью, как и для
--  create/edit/delete в V0031.
-- ════════════════════════════════════════════════════════════════════

UPDATE user_auth.policy
SET check_group_id = NULL
WHERE check_group_id = 2;
